import React from 'react';
import { Text, View } from 'react-native';
import { SIZES } from '../../constants';

const Account = () => {
    return (
        <View style={{
            padding: SIZES.body1 * 0.5
        }}>
            <Text>Remove Team</Text>
        </View>
    )
}

export default Account